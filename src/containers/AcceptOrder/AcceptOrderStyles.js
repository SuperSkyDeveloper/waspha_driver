import {StyleSheet} from 'react-native';
import {Metrics, Colors, AppStyles, Fonts} from '../../theme';

export default StyleSheet.create({
  container: {
    flex: 1,
  },

  acceptInfoWrap: {position: 'absolute', alignSelf: 'center'},
});
